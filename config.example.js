// config.example.js — ตัวอย่างไฟล์ config (commit ขึ้น GitHub ได้ ไม่มีค่าจริง)
// วิธีใช้: copy ไฟล์นี้เป็นชื่อ "config.js" แล้วใส่ค่า SUPABASE_URL / SUPABASE_KEY จริงของโปรเจกต์
// (หา 2 ค่านี้ได้จาก Supabase Dashboard > Project Settings > API)
const SUPABASE_URL = 'YOUR_SUPABASE_URL';
const SUPABASE_KEY = 'YOUR_SUPABASE_ANON_KEY';
